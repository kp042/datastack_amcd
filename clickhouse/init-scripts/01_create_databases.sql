-- =============================================================================
-- СОЗДАНИЕ БАЗ ДАННЫХ
-- =============================================================================
-- Выполняется автоматически при ПЕРВОМ запуске ClickHouse
-- =============================================================================

-- RAW — сырые данные из GA4/Firebase
CREATE DATABASE IF NOT EXISTS raw;

-- STAGING — промежуточный слой (dbt)
CREATE DATABASE IF NOT EXISTS staging;

-- MARTS — финальные витрины (dbt)
CREATE DATABASE IF NOT EXISTS marts;
